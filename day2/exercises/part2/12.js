'use strict';

let str = 'You cannot end a sentence with because because because is a conjunction';
let ans = str.replace('because '.repeat(3), '');

console.log(ans);

// Last Edit : 2023-02-24 14:01:58
