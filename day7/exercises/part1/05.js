'use strict';

function perimeterOfRectangle(length, width)
{
	let perimeter = 2 * (length + width);

	return (perimeter);
}

console.log(perimeterOfRectangle(3, 5));

// Last Edit : 2023-02-25 16:25:33
